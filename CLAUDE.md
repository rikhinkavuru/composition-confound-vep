# Project: Composition-Confound VEP Audit (MLCB 2025/2026 target)

Purpose of this file: persistent memory so future turns don't re-derive verified facts.
Read this FIRST every session before doing anything. Update it after every phase.
Original outline: notes/00_original_outline.md (source of truth for scientific plan; adjust here as we learn more, note deltas below).

## Status (update this line each turn)
PHASE: 1->2 (data acq mostly done, scoring/analysis starting). DONE: ClinVar+cCRE, Kircher, TraitGym, GLRB, features.py, C1/C2 features on ALL datasets (code/05, *_feat.parquet), TraitGym precomputed 10-gLM roster (code/06, data/traitgym/{mendelian,complex}_scored.parquet). IN PROGRESS: GPN-MSA 37GB download (data/gpn_msa/, PID bg, ~35min, for ClinVar/Kircher/GLRB which aren't in TraitGym). NEXT: Evo2-clinvar precomputed join for ClinVar noncoding; then E1-E6.
DECISION (locked): precomputed-first, NO local model inference. TraitGym ships authoritative per-variant LLR for evo2_1b/7b/40b, NT, Caduceus, HyenaDNA, SpeciesLM, AIDO, GPN-MSA + baselines (CADD 114-col incl GC/CpG/Roulette-MR/SpliceAI, phyloP, phastCons) — more credible + CPU-only than laptop MLM re-scoring. Task 3 (local DNABERT/NT) DELETED.

### SMOKE-TEST FINDING (2026-07-01, reshapes paper framing — READ)
Quick check on TraitGym matched benchmark (data/traitgym/*_scored.parquet): the composition/mutation confound is REAL but MODEST on the *matched* benchmark, exactly as outline predicted ("lower on TraitGym").
- H1: |LLR|-vs-confounder Spearman is MODEL-DEPENDENT: GPN-MSA rho~-0.16..-0.20 vs mu_5mer/Roulette-MR (alignment model tracks conservation); Evo2-40B rho~+0.22..+0.25 vs CpG/GC/flank_GC (single-seq autoregressive is GC-coupled); NT/Caduceus/HyenaDNA ~0. Magnitudes ~0.2, at low end of outline's 0.2-0.35.
- H2: composition-only GBM recovers LITTLE on matched (AUROC 0.52-0.57, AUPRC ~0.11-0.14 vs 0.10 base; recovery ratio ~0.2). Matching mostly works.
- Bonus: single-seq gLMs (NT/Caduceus/HyenaDNA) at/BELOW chance on TraitGym (AUROC 0.39-0.50 w/ |LLR|); only GPN-MSA/Evo2-40B/CADD/phyloP strong. ORIENTATION CAVEAT: must pick best of {LLR,-LLR,absLLR} per model per TraitGym convention — don't trust |LLR| blindly.
- IMPLICATION: the STRONG-confound headline must come from ClinVar (region-heterogeneous, unmatched) + the matched-vs-heterogeneous CONTRAST is the real thesis. This is an honest, calibrated, Koo-friendly framing (not "all gLM=confound"). ClinVar model scores = critical path now.

### THESIS CONFIRMED on ClinVar (2026-07-01, data/clinvar/clinvar_scored.parquet, code/07)
Built ClinVar noncoding scored table: 153,508 variants w/ C1/C2; 73,408 (47.8%) joined to goodarzilab/evo2-clinvar CSV precomputed scores (Evo2-7B/40B, GPN-MSA[36,811], CADD, NT-2.5B, phyloP-100/241/447/470way, SpliceAI, splice_proximity). Join on chrom(bare)+pos(start,1-based)+ref+alt; deduped transcripts. Label 127,937 Benign / 25,571 Pathogenic.
Smoke test on 73k Evo2-covered subset (path_rate 0.237): gLMs strong (Evo2-7B AUROC .961, Evo2-40B .964, GPN-MSA .961, CADD .986, phyloP .912). COMPOSITION-ONLY GBM (5-fold, C1/C2 only, zero seq context) = AUROC 0.708 / AUPRC 0.446 -> RECOVERY RATIO ~0.43-0.50 of every gLM's above-chance signal. H1: GPN-MSA |LLR| rho=-0.27 mu_5mer / -0.24 cpg (clear confound); Evo2-40B -0.18; Evo2-7B -0.13.
=> CONTRAST NAILED: composition recovers ~half of gLM AUROC-above-chance on unmatched ClinVar vs ~a fifth on matched TraitGym. Core thesis DE-RISKED + directionally confirmed. Paper viable.
CAVEATS: (1) nt_llr from evo2-clinvar CSV reads AUROC exactly 0.500 -> broken/constant join, DROP NT from ClinVar (NT covered properly in TraitGym). (2) CADD .986 is supervised/ClinVar-adjacent, treat as integrative reference not zero-shot gLM. (3) recovery ratios from quick 5-fold; real pipeline must use per-chrom CV + bootstrap SE + DeLong (outline §9). (4) ~47% of noncoding cCRE variants overlap CDS by gtf_feature -> consider a strict noncoding subset (exclude CDS-overlap) as robustness.

### E1-E4 DONE (real pipeline, code/10+11, analysis_utils.py, results/e{1,2,3,4}_*.csv)
analysis_utils.py: orient(), per_chrom_auc (sample-wt), composition_cv (leave-one-chrom-out HistGBM on C1/C2), bootstrap_auc_ci, partial_spearman (rank-residualize), delong_test (fast DeLong, self-tested), benjamini_hochberg. All self-tested OK.
E1 (|LLR|-vs-mu_5mer partial rho, control consequence): GPN-MSA most composition-coupled (-0.16 mend / -0.20 complex / -0.27 ClinVar); Evo2-40B -0.08..-0.11; Evo2-7B ~-0.04; NT/Caduceus/HyenaDNA ~0; phastCons -0.07..-0.14.
E2 (composition-only LOCO-CV recovery ratio = (comp-0.5)/(model-0.5), DeLong all p~0):
  - ClinVar UNMATCHED: comp AUROC 0.720 -> recovery 0.45-0.53 of EVERY strong model (GPN-MSA .48, Evo2-7B .48, Evo2-40B .47, CADD .45, phyloP .53).
  - TraitGym-mendelian MATCHED: comp 0.586 -> recovery 0.21-0.25.
  - TraitGym-complex MATCHED: comp 0.520(~chance) -> recovery 0.17-0.25; most gLMs AT CHANCE here.
  - GLRB eQTL: comp 0.596 >= GPN-MSA 0.560 (recovery 1.59) — zero-shot gLM near chance on causal eQTL, composition explains the little signal.
  HEADLINE: composition recovers ~2x more above-chance AUROC on unmatched (ClinVar ~0.48) than matched (TraitGym ~0.22).
E3a collapse: single-seq gLMs (NT/Caduceus) ~ZERO edge over composition in every |dGC| bin; GPN-MSA/Evo2-40B/CADD/phyloP keep stable ~0.24 edge. E3b composition-matched (NN-match pos<->neg on mu_5mer/dGC/cpg/ts/flankGC within consequence): gLM AUROC drops SMALL (ClinVar -0.006, mend Evo2-40B -0.019, NT -0.025); Kendall tau reorder mild (mend .905, complex .810). E4 Kircher within-element (21 loci): partial rho ~= raw rho (mean drop ~0) -> within fixed background composition leakage NEGLIGIBLE.
REFINED THESIS (honest, less overclaim, Koo-aligned): the composition/mutation confound INFLATES APPARENT performance on unmatched/aggregate benchmarks (recovers ~half above-chance AUROC) and bites SINGLE-SEQUENCE gLMs HARDEST (NT/Caduceus/HyenaDNA barely beat composition anywhere, at chance on matched); alignment-based (GPN-MSA) + large autoregressive (Evo2-40B) + conservation retain composition-independent signal that survives matching. Confound is across-set/ascertainment, NOT within-element. This differentiated model x benchmark x regime MAP is the contribution (richer than "gLM=composition").
### E5-E6 DONE (code/12, results/e5_calibration.csv, e6_surviving.csv, *_scored_ctx.parquet cache w/ context5)
calibrate_llr(): LOCO cross-fit per-5-mer-context neutral mean from CONTROL variants, subtract (GPN-Star-faithful, model-agnostic, no leakage). context5 = hg38 pentamer + '>' + alt via pysam.
E5: post-hoc calibration HALVES mutation-rate coupling of most-affected model (GPN-MSA mu-corr -0.33->-0.17 ClinVar) at negligible AUROC cost (.958->.955); Evo2-40B .963->.958; leaderboard Kendall tau 0.80 (ClinVar) / 0.905 (TraitGym) = MILD reorder. Effect small because (per E3/E4) models carry real signal — calibration is a decontamination DIAGNOSTIC not a performance killer. Honest framing.
E6: gLM-wins (calibrated GPN-MSA ranks positive high, composition misses) vs composition-wins: TF-motif/RemapTF overlap OR=5.4 (mendelian) / 2.3 (complex) -> surviving gLM signal concentrates on MOTIF-DISRUPTING variants (H5 confirmed). Splice/comp-neutral enrichment noisy (few splice variants in noncoding-reg set).

### CRYSTALLIZED PAPER CLAIMS (final, evidence-backed — the spine, do NOT drift from these)
1. Context-free composition/mutation classifier (C1/C2, no seq, no model) hits AUROC 0.72 on ClinVar noncoding, recovers ~45-53% of above-chance AUROC of EVERY leading model (GPN-MSA/Evo2/CADD/phyloP). [E2]
2. Careful matching shrinks recovery to ~20-25% (TraitGym); the ~2x unmatched->matched contrast quantifies composition-attributable leaderboard performance. [E2]
3. Confound is MODEL-HETEROGENEOUS: single-seq gLMs (NT/Caduceus/HyenaDNA) ~= composition baseline (at chance on matched, zero edge per bin); alignment-based (GPN-MSA)+large autoregressive (Evo2-40B)+conservation retain signal surviving composition-matching. [E1,E3a,E3b]
4. Confound is across-set/ascertainment, NOT within-element: Kircher fixed-background partial rho ~= raw rho. [E4]
5. Post-hoc pentanucleotide calibration halves the most-coupled model's mutation-rate correlation at negligible AUROC cost + mild leaderboard reorder; released calibrate_llr(). [E5]
6. Surviving gLM-over-composition signal enriched for TF-motif disruption (OR 2.3-5.4). [E6]
Core contribution = composition/mutation confound DECOMPOSITION of zero-shot gLM VEP, quantified across models x benchmark regimes, + calibration diagnostic + surviving-signal map. Gupta-genre structure, Koo-aligned (single-seq gLM ~ trivial baseline). NOT breadth-sprawl.
### FIGURES + PAPER DONE (draft v1)
Figures: code/13_figures.py -> results/figures/fig{1..4}.{pdf,png} (fig1 recovery headline, fig2 confound+collapse, fig3 calibration, fig4 within-element+surviving). Copied to paper/figures/.
Paper: paper/main.tex (jmlr pmlr class) + paper/references.bib (16 refs, DOIs from verified lit list). Compiles via `cd paper && tectonic main.tex` -> main.pdf. 8 pages TOTAL (main text pp1-6, References pp7-8) => WITHIN 8pp-excl-refs limit with ~2pp main-text budget to spare. Single-blind format (author placeholder "Author Name"/Institution — user fills real names). Structure mirrors Gupta confound genre: intro/confound-formalism/setup/E1-E6 results w/ 4 figs/discussion+limitations+recommendations. All numbers pulled from results/e*.csv. Only cosmetic LaTeX warnings (overfull hbox, algorithm2e UTF-8) — render verified clean pp1+4.
### DRAFT v2 — AUDIT CLEAN (2026-07-01, paper-audit skill, ledger.json)
Added Table 1 (main results per model x benchmark). Ran paper-audit fix-loop -> GATE CLEAN at iteration 2 (0 blocking; 3 major + 6 minor + 1 nit all resolved/waived). Fixes applied: (F001) Reproducibility/availability statement w/ data sources + GitHub URL placeholder; (F002) strict-noncoding robustness RAN + added (exclude 46,852 CDS-annotated -> 106,656 SNVs; comp AUROC 0.720->0.734, recovery 0.48-0.50 UNCHANGED -> confound not a coding artifact); (F003) HistGBM config+feature list; (F004) "ten"->"nine" gLMs; (F005) Table ref; (F006) acronyms expanded; (F007) entropy2026 cited; (F009 MAJOR honesty) "statistically indistinguishable"->"do not exceed composition baseline" (DeLong shows NT/Caduceus sig WORSE than comp on mendelian p=.03/1e-3); (F010) E6 OR small-n caveat. Paper now 8pp main + refs pp9-10 = AT 8pp-excl-refs limit, compiles clean.
STATUS: DONE + HARDENED + PUBLISHED-TO-GITHUB. Submission-ready. paper/main.pdf = 7pp main + refs pp8-9, 11pt/1in-margins/single-column (CFP-compliant, geometry override of jmlr's 1.25in), audit-clean. Author = Rikhin Kavuru, Independent Author (rikhinkavuru@gmail.com). Repo LIVE: github.com/rikhinkavuru/composition-confound-vep (nested git repo inside ~/mlcbfinal; home ~ is separate repo — only run git from ~/mlcbfinal). Submission subject areas advised: PRIMARY Regulatory genomics; SECONDARY General ML + Statistical genetics. Full-paper PMLR track opt-in for rebuttal.
HARDENING loop iterations done (autonomous, no breadth): 1in margins; genome-wide neutral E1 (context-mean GPN-MSA vs Carlson rate rho=0.63, 3072 contexts, code/14+results/e1_neutral_context.csv); E2 significance (non-overlapping bootstrap CIs across matched/unmatched); E5 genome-neutral corroboration (0.958->0.961, coupling 0.33->0.18); Table 1 per-chrom stratified-bootstrap 95% CIs on every AUROC (code/15, analysis_utils.per_chrom_auc_ci). All pages visually verified clean. NO breadth added (per user).
REMAINING = human-domain only: (1) user proofread of paper/main.pdf; (2) actual CMT submission + bioRxiv/arXiv preprint. Further model/dataset breadth explicitly OUT of scope per user.
CONTINUING /loop polish (goal = absolute best):
- DONE: code/calibrate.py = clean standalone released calibrate_llr() (genome + control estimators) making the paper's "released transform" claim real. README.md written (repo overview + reproduce steps). Prose humanizer pass = clean (no em-dashes/filler/negative-parallelism, verified by grep).
- DONE: E5 genome-wide neutral table (code/14, block-sampling 250x50kb windows = 35.5M GPN-MSA scores, all 3072 contexts, ~40s; results/neutral_5mer_gpnmsa.csv + e5_genome_neutral_gpnmsa.csv). RESULT (stronger than control-based): genome-neutral calibration of GPN-MSA on ClinVar cuts mu-coupling 0.333->0.178 while AUROC UNCHANGED/slightly up (0.958->0.961) => "decontaminates at no cost". Added strengthening sentence to E5. (NOTE: code/14 had an off-by-one in pentamer center index — pysam fetch 0-based, center off = p-start+2 not +3; fixed. If reusing block-sampling from tabix+fasta, mind the 0-based offset.) Paper still 8pp main, compiles clean.
- USER-FILL placeholders in paper: real author names/affiliation (single-blind = names stay ON pdf) + real GitHub repo URL (currently github.com/ANONYMIZED/composition-confound-vep).
- Remaining optional depth (only if pursuing further): neutral-site genome-wide E1 correlation; strict-noncoding as its own column in Table 1; indel extension (Evo2/GPN handle). Do NOT add breadth beyond core confound-decomposition contribution.
TraitGym DONE (data/traitgym/{mendelian,complex}_traits_test.parquet). GLRB eQTL DONE — see full entry below.
features.py DONE + tested (27/27 pass) — see full entry below.

### GLRB eQTL (DONE, code/02_get_glrb_eqtl.py, data/glrb_eqtl/)
- Source: InstaDeepAI/genomics-long-range-benchmark, task `variant_effect_causal_eqtl`, All_Tissues.csv (has CHROM,POS,SNP,REF,ALT,label,split,tissue,distance_to_nearest_TSS). `datasets`>=4 dropped script-datasets so we reimplement the handler directly (see script docstring).
- REFACTORED from upstream: original script materialized full 100kb ref+alt sequence strings per variant → ~17.7GB RAM list on 16GB machine → swapped to disk, crawled, filled storage (THIS crashed the prior session). Now stores LIGHTWEIGHT variant table only (chrom,pos,ref,alt,label,tissue,distance_to_nearest_tss); 100kb windows extracted lazily at scoring time from indexed hg38 (models need far smaller contexts anyway). Boundary-filter (drop variants whose 100kb window runs off chromosome) preserved via .fai index length, no extraction. REF pulled from indexed hg38 — verified 0/88891 mismatch vs shipped CSV REF (coord alignment exact).
- Counts: raw 97,922 → train 88,891 (expected ~88,717, +0.2%, dropped 169 boundary), test 8,850 (expected ~8,846, dropped 12 boundary). 49 tissues. Labels ~balanced (train 44623 neg/44268 pos). Test split = chr9+chr10 ONLY (GLRB held-out-chrom design). Files tiny: train.parquet 825KB, test.parquet 93KB.
- Output: data/glrb_eqtl/{train,test}.parquet. chrom is "chr1" style (matches hg38.fa for features.py). pos 1-based VCF. Note: hg38.fa cached at ~/.cache/glrb_hg38/hg38.fa is a DUPLICATE of data/reference/hg38.fa (3.2GB each) — could symlink to reclaim, low priority. Redundant .gz files already deleted.

### code/features.py (DONE, opus-built, tested)
Entry point: `compute_features(df, ref_fasta_path, window_sizes=(11,21,51), seq_col=None, mutation_rate_path=None, n_jobs=1) -> pd.DataFrame`. Input df needs chrom/pos/ref/alt (1-based VCF coords).
Mutation-rate source: **Carlson et al. 2018 Nat Commun 9:3753** (DOI 10.1038/s41467-018-05936-5), pentanucleotide ERV-derived rates, table at data/mutation_rates/ (provenance in SOURCE.md), pulled from `stephenrong/mutation-paper` GitHub mirror, 3072 entries (1024 contexts x 3 alts). Chose over Roulette (too large, position-model not context-lookup) — documented rationale in SOURCE.md.
Reference genome: hg38 fully downloaded + pysam-indexed at data/reference/hg38.fa(.fai) — DONE, no follow-up needed.
Output cols (19, appended to input): dGC_w{11,21,51}, dkmer{1,2,3}_w{11,21,51} (9 cols), mu_5mer, cpg_ref, cpg_alt, is_transition, flank_gc_w51, QC flags ref_match + feat_ok.
Design: ALL features strand-invariant by construction (GC/CpG palindromic, Carlson table strand-symmetric, ts/tv symmetric, k-mer L1 invariant under revcomp) — no strand-averaging needed for C1/C2 (this is separate from gLM LLR scores, which per outline DO need strand-averaging — no conflict, different objects).
Speed: ~3000 variants/s single-core real hg38 (~6min per 10^6). n_jobs parallelism only pays off at large N (fork overhead dominates at small N) — use n_jobs=1 for datasets <100k, revisit if full ClinVar noncoding (153k) run feels slow.
Tests: code/test_features.py, 27/27 pass, incl. hand-verified k-mer deltas, 500-case fuzz vs brute-force oracle, ts/tv truth cases, CpG detection, monotonicity sanity check (CpG C>T rate 0.134 vs A>C transversion 0.0019, >10x — biologically correct direction).
READY to import into all 4 dataset feature-annotation steps now.

### ClinVar + ENCODE cCRE (DONE, code/03_get_clinvar.py, data/clinvar/)
- clinvar.vcf.gz (192MB) + .tbi downloaded from NCBI FTP, gzip-verified OK. GRCh38-cCREs.bed downloaded from wenglab (2,348,854 cCREs, matches expected count exactly).
- bedtools 2.31.1 installed via brew (was missing) - used for intersection, no pure-python fallback needed.
- Real counts: 4,439,382 total VCF records -> 4,134,544 SNVs (single ref/alt base, ACGT only) -> 634,433 with >=2-star review status -> 342,200 after P/LP vs B/LB labeling (dropped VUS/conflicting) [280,462 Benign, 61,738 Pathogenic] -> 153,508 overlap >=1 ENCODE cCRE (noncoding regulatory: 92,920 dELS, 26,377 pELS, 10,710 PLS, rest CA/TF variants). NOTE: 153,508 is somewhat higher than the outline's "~10^5" ballpark but same order of magnitude - flag in paper, not a red flag.
- Output: data/clinvar/clinvar_noncoding_labeled.parquet (342,200 rows, all labeled variants incl. coding; filter on `in_ccre`==True for the noncoding-regulatory subset). Columns: chrom,pos,ref,alt,clnsig,clnrevstat,label,variant_id,ccre_class,in_ccre.

### Kircher satMutMPRA (DONE, code/04_get_kircher_mpra.py, data/kircher_mpra/)
- Source: bundled data/elements.tsv.gz from kircherlab/MPRA_SaturationMutagenesis GitHub repo (site source code), NOT a tagged release asset - found via git tree listing. No scraping needed.
- IMPORTANT: bundled file has 29 construct-level "Element" labels (more than the 21 canonical loci) because TERT (x4 conditions), PKLR (x2), LDLR (x2), SORT1 (x3), ZRS (x2) each have multiple assayed constructs bundled in from follow-up work hosted on the same site. All 21 canonical loci (10 promoter+10 enhancer+UC88) ARE present, verified programmatically - none missing.
- File also had BOTH GRCh37 and GRCh38 coordinates for every row (Release column) - filtered to GRCh38 only (project convention). 22 rows total (11 per build) had unparseable NaN coordinates, dropped.
- Real counts: 89,316 raw rows (both builds, all constructs) -> 44,658 GRCh38-only -> 44,647 after dropping NaN-coord rows -> 33,517 unique physical (chrom,pos,ref,alt) variants when collapsing across constructs of the same locus. Final combined table kept at construct-level granularity (44,647 rows) since different conditions of the same locus have genuinely different measured effects. 39,170 SNVs + 5,477 1bp deletions. This is higher than the outline's "30k+" figure but the 33,517 unique-variant count is close - the extra rows come from multi-condition reassay of the same physical positions (TERT/PKLR/SORT1/ZRS/LDLR), not new elements.
- Output: data/kircher_mpra/satmut_mpra_combined.parquet. Columns: element_name (construct-level, 29 distinct), locus (canonical-21 grouping), chrom, pos, ref, alt ("-" = 1bp deletion), log2_effect (from raw "Coefficient" column - a regression coefficient from the site's model, treated as the log2-scale effect size per outline; not independently re-derived), pvalue, n_barcodes, dna_count, rna_count, is_deletion.

## Verified facts (locked in, do not re-derive)

### Venue format (VERIFIED via live web fetch, fable agent, 2026-07-01)
- MLCB = Machine Learning in Computational Biology. MLCB 2025 = 20th meeting, NYGC, Sep 10-11 2025, published PMLR v311. Editors: David A. Knowles & Peter K. Koo.
- Page limit: **8 pages EXCLUDING references** for full paper track. Exceeding = auto-reject. This matches user's ask exactly, no adjustment needed.
- Submission format: 11pt font, 1in margins, single-column preferred. NOT a mandated class at submission time.
- Camera-ready / PMLR style: `\documentclass[pmlr]{jmlr}` (JMLR/PMLR class, CTAN `jmlr` pkg, github.com/JmlrOrg/jmlr-style-file). Sample: proceedings.mlr.press/assets/examples/pmlr-sample-single-column.pdf. Draft directly in this template to avoid reformatting later.
- **Review is SINGLE-BLIND** (confirmed verbatim from CFP: "no longer asking authors to anonymize... reviewing is single blind not double blind"). Author names STAY ON the PDF. Matches user's ask.
- Two tracks: 8pp full paper (can opt into PMLR pub, only opt-in gets rebuttal) vs 2pp abstract/poster (not PMLR-eligible). We target full 8pp + opt-in.
- Appendix allowed, outside 8pp count, but main paper must stand alone.
- No broader-impact/reproducibility-checklist section required. Code/data release encouraged not mandated (we should still release, it strengthens the paper per genre norms).
- Submission via Microsoft CMT (not relevant to drafting, just FYI).
- Preprinting to bioRxiv/arXiv in parallel is fine.

### Reviewer taste / genre precedent (VERIFIED)
- **Closest structural template: Gupta, Moses, Lu, "Representation Learning Methods for Single-Cell Microscopy are Confounded by Background Cells"**, PMLR 311:162-177, bioRxiv 10.1101/2025.06.26.661577. Real, confirmed MLCB 2025. Structure to mirror: (1) name confound (background cells leak into embeddings) (2) build clean eval framework (synthetic crops, swapped backgrounds) (3) measure damage on 3 real models (localization acc drop up to 15.8%) (4) show it propagates to a downstream bio task (5) propose fix (background-masking/invariant features). Code released (microsoft/microscopy-sc-robustness).
- Secondary precedent: Schuster, "Can sparse autoencoders make sense of gene expression latent variable models?" PMLR 311:81-94 — simulate ground truth, find where interpretability method fails, ship partial fix tool (scFeatureLens).
- Confound/critique genre is THIN in v311 (Gupta is the one airtight match) — good, means low direct competition, but also means we should lean hard into mirroring Gupta's rhetorical structure since it's the proven-accepted shape.
- **Peter Koo IS a v311 volume editor.** His lab's Genome Biology 2025 paper (Tang, Somia, Yu, Koo; DOI 10.1186/s13059-025-03674-8) verdict: frozen pretrained gLM embeddings give NO substantial advantage over one-hot on 6 cell-type-specific regulatory tasks; supervised-from-scratch one-hot models match/beat pretrained gLMs; foundation-model framing doesn't hold up yet for noncoding gLMs. This is directly favorable priors for our thesis — editor is on record skeptical of gLM representational claims. Cite this paper prominently, frame our zero-shot-VEP finding as the "variant-level, VEP-specific" analog of his "representation-level, task-specific" finding.
- Deadline calibration (2026 cycle): submit ~Jul 3 AOE, decisions ~Aug 14, meeting Nov 16-17 2026. Not urgent this exact week but should treat as real deadline pressure once we know current-cycle date.

## Lit fact-check (VERIFIED, all 11 citations real, no fabrications, 2026-07-01)
All numbers in outline confirmed accurate. Corrections to use in paper text:
1. Mechanistic Invariance Test = arXiv:2604.06549 (GEM/ICLR 2026 workshop). r=0.78-0.96 AT-content, PWM CSS=1.00/SCR=0.98, confirmed.
2. Region-heterogeneity paper full title "Genomic heterogeneity inflates THE PERFORMANCE OF variant pathogenicity predictions", bioRxiv 2025.09.05.674459, Lu/Liu/Lin/Brandes. Evo2 AUROC 0.975 agg -> 0.903 non-splice intron / 0.697 canonical splice (use these exact numbers, not vague "0.6-0.9").
3. GPN-MSA: Nat Biotechnol 43:1960-1965 (2025), DOI 10.1038/s41587-024-02511-w. HF `songlab/gpn-msa-hg38-scores` confirmed exact wording "~9 billion possible SNVs."
4. GPN-Star: PMC12458161, bioRxiv 2025.09.21.677619, "Predicting functional constraints across evolutionary timescales with phylogeny-informed genomic language models." rho=0.31-0.34 chr22 vs Roulette, verbatim confirmed; calibration -> below PhyloP/PhastCons.
5. TraitGym: bioRxiv 2025.02.11.637758. 113 Mendelian + 83 complex traits confirmed exact. Complex positives PIP>0.9 AND p<5e-8 genome-wide sig (extra filter beyond what outline said - ADD THIS). Controls PIP<0.01. Matching: chr+consequence+distance-TSS both; complex ALSO +MAF+LD. matched_9 = 9:1 control ratio.
6. Evo2: Nature 2026, DOI 10.1038/s41586-026-10176-5, Brixi et al, Hsu/Hie senior. StripedHyena2, up to 1Mbp context, ~9T bp training.
7. Blind Spots: full title "Benchmarking DNA Foundation Models: Biological Blind Spots in Evo2 Variant-Effect Prediction", bioRxiv 2026.03.10.710786 (DOI prefix 10.64898 not 10.1101), Mathur & Sachidanandam. 65.8%(52/79)->5.1%(4/79) sensitivity confirmed exact, specificity rises to 93.8%.
8. Genomics Long-Range Benchmark: OpenReview Cdc90HKs1I, InstaDeepAI. Zero-shot DNA LMs 0.50-0.52 AUROC causal eQTL; CADD=0.56, Enformer=0.76 beat them (use these exact numbers).
9. Kircher satMutMPRA: Nat Commun 10:3583 (2019), DOI 10.1038/s41467-019-11526-w. Title says "twenty" but Methods says 21 elements (10 promoter+10 enhancer+UC88) - cite as 21, footnote the title/methods discrepancy. 30k+ SNVs/1bp dels, GC 28-73%, length 187-601bp all confirmed verbatim.
10. Entropy/Fisher: arXiv:2604.04287, "Entropy, Disagreement, and the Limits of Foundation Models in Genomics", Rochkoulets/Vrček/Šikić, ICLR 2026 LMRL workshop. Fisher info concentrates in embedding not transformer layers.
11. Koo Genome Biology 2025, DOI 10.1186/s13059-025-03674-8 - confirmed separately already.
No adjustments needed to outline's scientific claims — outline author did their homework. Proceed with full confidence on H1-H5 framing.

## Dataset feasibility (VERIFIED, 2026-07-01) — concrete access plan
1. **GPN-MSA scores**: HF `songlab/gpn-msa-hg38-scores`, bgzip+tabix TSV (chrom,pos,ref,alt,score), ~9B SNVs, tens of GB total. DO NOT bulk download — tabix-query remote HTTPS directly: `tabix https://huggingface.co/datasets/songlab/gpn-msa-hg38-scores/resolve/main/scores.tsv.bgz chr:start-end` per our variant list, or wget once + local tabix. Public, no auth.
2. **ClinVar**: `https://ftp.ncbi.nlm.nih.gov/pub/clinvar/vcf_GRCh38/clinvar.vcf.gz` (~183MB) + .tbi. CLNREVSTAT field for stars (>=2star = "criteria_provided,_multiple_submitters,_no_conflicts" / "reviewed_by_expert_panel" / "practice_guideline"). ENCODE cCRE: `wget https://downloads.wenglab.org/Registry-V4/GRCh38-cCREs.bed` (2,348,854 cCREs) + class-specific `.PLS.bed`/`.ELS.bed`. bedtools intersect to annotate region type. Public.
3. **TraitGym**: HF `songlab/TraitGym` via `load_dataset("songlab/TraitGym","mendelian_traits"/"complex_traits",split="test")`. Ships variant lists + labels + matched_9 controls + PRECOMPUTED reference model scores (GPN-MSA, CADD, Enformer, Borzoi, Evo2 already scored!). GitHub `songlab-cal/TraitGym`. Public.
4. **Kircher satMutMPRA**: processed log2-effect tables (NOT raw counts, no reconstruction needed) at kircherlab.bihealth.org/satMutMPRA/ and mpra.gs.washington.edu/satMutMPRA/, or bulk via GitHub `kircherlab/MPRA_SaturationMutagenesis` tagged release. Public.
5. **GTEx eQTL / Genomics Long-Range Benchmark**: HF `InstaDeepAI/genomics-long-range-benchmark`, task `variant_effect_causal_eqtl`, ~88,717 train/8,846 test, 49 tissues, ref/alt seq + label + tissue + chrom/pos + dist-TSS. Positives=GTEx SuSiE PIP>0.9, negatives=Avsec/Enformer matched PIP<0.01. Summary-stat derived, NOT raw genotypes -> NO dbGaP gating. Public via `load_dataset`.
6. **Evo2 precomputed scores**: `jang1563/evo2-spaceflight-vep-scores` is NARROW (10 genes only, has noncoding_scores w/ TERT MPRA + ClinVar slice) - insufficient alone. Use `goodarzilab/evo2-clinvar` instead for broad ClinVar (>=2star, Evo2-7B+40B, NT, ESM-2, GPN-MSA, CADD, phyloP, SpliceAI all precomputed - ideal drop-in). Zenodo 19701997 (EVEE/goodfire.ai) = 4.25M ClinVar variants w/ Evo2-7B embedding score, 5 parquet shards ~7GB each, for genome-scale option. All public.
7. **Local CPU/MPS models**: DNABERT-2 117M (`zhihan1996/DNABERT-2-117M`, mirror `magicslabnu/DNABERT-2` if triton issues, force eager attn) = RECOMMENDED, feasible few hrs for 100-150k seqs. NT-v2 use 50m/100m variants only locally (`InstaDeepAI/nucleotide-transformer-v2-50m-multi-species`), 500m -> precomputed only. Caduceus (`kuleshov-group/caduceus-ph_seqlen-131k_d_model-256_n_layer-16`) tiny (~2-8M) but Mamba kernels slow-fallback on CPU, still tractable; run fwd+revcomp, average.
No dbGaP/license gates anywhere in this plan.

## Compute environment (VERIFIED)
- System python3 (`/opt/homebrew/bin/python3`, 3.14) has NOTHING installed — do not use it.
- **Use `~/Downloads/venv/bin/python`** (python3.14) — pre-existing venv from prior project, has numpy 2.4.6, scipy 1.17.1, pandas 3.0.3, sklearn 1.8.0, lightgbm 4.6.0, matplotlib 3.10.9, Bio(python) 1.87, requests, huggingface_hub 1.16.4, torch 2.12.0, transformers 5.9.0. Added pysam 0.24.0, h5py 3.16.0, statsmodels 0.14.6 into this same venv (missing before, now installed) — safe, this venv is general-purpose scratch, not project-pinned.
- Machine: Mac arm64 (Apple Silicon), 10 CPU cores, 16GB RAM, no CUDA GPU. torch present = CPU/MPS only. Budget accordingly: DNABERT-2 (117M) feasible; NT-v2/Caduceus feasible if small variant; avoid anything requiring >~a few GB VRAM equivalent or hour-scale single-batch GPU compute — prefer precomputed scores for big models (GPN-MSA, Evo2).
- Internet access confirmed working (huggingface.co, pypi.org reachable via curl).

## LaTeX toolchain (SOLVED, 2026-07-01)
- `brew install --cask basictex` FAILS in this environment — needs interactive sudo password, not available headless. Do not retry.
- **Use `tectonic`** (already installed via brew, `/opt/homebrew/bin/tectonic`, sudo-free, self-contained, auto-fetches packages from its own bundle mirror including `jmlr.cls`/`pmlr` option — no manual .dtx/.ins build needed). Confirmed: `tectonic pmlr-sample.tex` in `paper/jmlr_pkg/jmlr/` compiles clean end-to-end (11pp PDF) with `\documentclass[pmlr]{jmlr}`.
- Build command for our paper: `tectonic paper/main.tex` (run from repo, or `cd paper && tectonic main.tex`). Ignore benign "Invalid UTF-8 byte" warnings from algorithm2e.sty (upstream cosmetic) and BibTeX "errors ignored" warning unless refs actually fail to render — verify visually.
- Template source: `paper/jmlr_pkg/jmlr/pmlr-sample.tex` (full example w/ theorems, tables, figures, siunitx, booktabs, longtable already demoed) + `jmlr.pdf` (the class doc, in same dir) — read jmlr.pdf's relevant sections for `\Name`/`\Email`/`\addr`/`\jmlrvolume`/`\jmlrworkshop` macro usage before drafting main.tex.
- Page-count check: use `~/Downloads/venv/bin/pip install pypdf` then `from pypdf import PdfReader; len(PdfReader(path).pages)` to verify 8pp-excl-refs compliance before every "final" draft.

## Working conventions
- This dir (`~/mlcbfinal`) is a plain project dir, NOT yet a git repo of its own — home dir (`~`) is a separate unrelated git repo with unrelated pending changes (ragversate/calma-strategy deletions). DO NOT run git commands from here that could touch the home repo. If we want version control for this project, `git init` fresh inside `~/mlcbfinal` only, after asking user.
- Directory layout: paper/ (LaTeX+PDF), code/ (all scripts), data/ (raw+processed, gitignore-able, large), results/ (figures, tables, stats), notes/ (working notes, verified-fact logs), logs/ (agent run logs).
- All heavy compute must be CPU/MPS-feasible on a Mac laptop — no CUDA assumed. Prefer precomputed scores over local reimplementation wherever released.
- Caveman mode active globally (terse). Chat responses terse; code/commits/paper prose normal register (paper text must read as normal academic English, caveman style does NOT apply to paper content).
- Orchestration: fable model for reasoning/orchestrating agents, sonnet-5/opus-4.8 for worker subagents doing focused execution, per user's explicit instruction.
- Session has a persistent /goal stop-hook: don't stop until user's full instruction is satisfied (paper done to best of ability, then continuous /loop self-improvement). Keep this file updated so a compacted/new context can resume without re-deriving.
